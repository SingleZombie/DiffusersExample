{
    "base": {
        "logging_dir": "logs",
        "output_dir": "models/ddpm",
        "checkpointing_steps": 5000,
        "valid_epochs": 2,
        "train_dataset_name": "merkol/ffhq-256",
        "resolution": 32,
        "train_batch_size": 64,
        "dataloader_num_workers": 1,
        "num_epochs": 50,
        "resume_from_checkpoint": "latest"
    },
    "ddpm": {
        "model_config": "unet.json",
        "learning_rate": 1e-4,
        "valid_batch_size": 4,
        "valid_loops": 100,
        "ddpm_num_inference_steps": 100
    }
}